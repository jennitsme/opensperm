# Policies

- `policy.schema.json` (planned): declarative RBAC/ABAC for tools + data scopes.
- `examples/`: sample policies for local dev, offline mode, and restricted egress.

TODO: define schema + validator, integrate with CLI (`opensperm policy check`).
