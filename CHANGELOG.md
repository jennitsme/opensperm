# Changelog

## v0.1.0-pre
- Runtime: sandbox with egress allowlist, rlimits, streaming IPC; policy/approvals (file, prompt, approve_all, webhook); logging to file via env.
- CLI: init/run/test/package/policy-check; run-config + registry loader; transcript runner executes via sandbox.
- SDKs: TS/Rust stubs; manifest/IP C schemas; signing helpers.
- Demo: echo, uppercase, reverse, http_mock tools with plan/transcript.
- CI: fmt, clippy, test.
