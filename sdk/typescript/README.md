# TypeScript SDK
Goals:
- Define tools/skills with typed schemas (zod-style) and metadata (capabilities, scopes, version).
- Middleware: auth, logging, policy enforcement hooks.
- Contract tests: run skills against runtime shims + golden transcripts.
- Packaging/signing: produce signed bundles for runtime verification.

Next steps:
- Specify manifest + schema types.
- Implement tool definition helpers + contract test harness.
