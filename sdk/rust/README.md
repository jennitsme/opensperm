# Rust SDK
Goals:
- Derive macros for tool contracts with strict types.
- Middleware hooks for auth/logging/policy.
- Contract test harness against runtime shims.
- Bundle signer/validator aligned with runtime verification.

Next steps:
- Define manifest structs + serde schemas.
- Implement derive macros + test harness skeleton.
