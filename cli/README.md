# CLI
Planned commands:
- `opensperm init skill` — scaffold a typed skill (TS or Rust) with manifest + tests.
- `opensperm run` — run agent locally with sandbox/policy config.
- `opensperm test` — run contract tests + golden transcripts.
- `opensperm package --sign` — build + sign skill bundles.
- `opensperm policy check` — validate policies against schema.

Next steps:
- Define command surface + config file format.
- Wire to runtime APIs once core stabilizes.
